<html>
    <html lang="en">

    <head>
        <meta charset="UTF-8">
        <link rel="stylesheet" href="/css/main.css" type="text/css">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>Rare Car Imports</title>
        <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>
        <script src="/js/updateCustomer.js"></script> 
    </head>
    
    <body>
        <header>
            <h1>Cars</h1>
        </header>
    
        <nav>
            <a href="/">Home</a>
            <a href="/diagram">Schema Diagram</a>
            <a href="/cars">Cars</a>
            <a href="/customers">Customers</a>
            <a href="/locations">Locations</a>
            <a href="/transactions">Transactions</a>
            <a href="/transactionCars">Transactions Details</a>
        </nav>
        <main id="update" style="display: block">
            <form method="POST" id="updateCustomer">
                <legend><strong>Update Customer</strong></legend>
                <fieldset class="fields">
                    <label> <strong>Updating:</strong> </label> 
                    <select name="input-customer" id="input-customer-ajax">           {{! handlebars dynamically filled dropdown for selecting which customer to edit }}
                        <option value="">Select a Customer</option>
                        {{#each data}}
                        <option value="{{this.customerID}}">{{this.name}}</option>    {{! shows the name in the dropdown}}
                        {{/each}}
                    </select>
                    <br>
                    <label> Name: </label>        {{! value for each input is put into the handlebars variable}}
                    <input type="text" id="customerName" name="Name" value="{{this.name}}" required>
                    <br>
                    <label> Contact Number: </label> 
                    <input type="text" id="customerContactNumber" name="ContactNumber" value="{{this.contactNumber}}" required>
                </fieldset>
                <br>
                <button type="submit" class="delete-btn" >Update Customer</button>       {{! onClick submits the POST form in which updateCustomer.js takes over }}
                <a href="/customers">Cancel</a>                                          {{! reroutes back to customers.hbs }}
            </form>

        </main>
    </body>
</html>